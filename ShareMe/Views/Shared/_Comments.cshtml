@using ShareMe.Core.Models

@model ICollection<ShareMe.Core.Models.Comment>

@addTagHelper *, ShareMe

@foreach (var comm in Model)
{ 
	<comment Commenter="@comm.User.UserName">@comm.Message</comment>
}